// frontend/src/environments/environment.ts
export const environment = {
  production: false,
  // Change this to match your backend's URL where it's running (e.g., http://localhost:5000/api)
  apiUrl: 'http://localhost:5000/api'
};